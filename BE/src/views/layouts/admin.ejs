<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= title || 'Admin Dashboard' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"
    ></script>
  </head>
  <body
    class="flex bg-slate-100 min-h-screen text-slate-800"
    x-data="{ openMenu: null }"
  >
    <!-- Sidebar -->
    <aside class="w-64 bg-white shadow-md flex flex-col fixed inset-y-0">
      <div class="p-4 border-b flex items-center justify-between">
        <h2 class="text-xl font-bold text-emerald-600">🚗 Sửa Xe</h2>
      </div>

      <nav class="flex-1 p-4 space-y-2 overflow-y-auto">
        <a
          href="/admin/dashboard"
          class="block px-4 py-2 rounded hover:bg-emerald-100 font-medium"
          >🏠 Bảng điều khiển</a
        >

        <!-- Dropdown Quản lý -->
        <div>
          <button
            @click="openMenu === 1 ? openMenu = null : openMenu = 1"
            class="w-full text-left px-4 py-2 rounded hover:bg-emerald-100 font-medium flex justify-between items-center"
          >
            🧰 Quản lý
            <svg
              :class="{'rotate-180': openMenu === 1}"
              class="w-4 h-4 transition-transform"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M19 9l-7 7-7-7"
              />
            </svg>
          </button>
          <div
            x-show="openMenu === 1"
            x-collapse
            class="mt-1 ml-6 space-y-1 text-sm"
          >
            <a
              href="/admin/employees"
              class="block px-3 py-1 rounded hover:bg-emerald-50"
              >Thợ sửa xe</a
            >
            <a
              href="/admin/workshifts"
              class="block px-3 py-1 rounded hover:bg-emerald-50"
              >Ca làm việc</a
            >
            <a
              href="/admin/services"
              class="block px-3 py-1 rounded hover:bg-emerald-50"
              >Dịch vụ</a
            >
            <a
              href="/admin/parts"
              class="block px-3 py-1 rounded hover:bg-emerald-50"
              >Phụ tùng</a
            >
            <a
              href="/admin/part-types"
              class="block px-3 py-1 rounded hover:bg-emerald-50"
              >Loại phụ tùng</a
            >
            <a
              href="/admin/suppliers"
              class="block px-3 py-1 rounded hover:bg-emerald-50"
              >Nhà cung cấp</a
            >
            <a
              href="/admin/purchase-orders"
              class="block px-3 py-1 rounded hover:bg-emerald-50"
              >Phiếu nhập hàng</a
            >
          </div>
        </div>

        <a
          href="/admin/bookings"
          class="block px-4 py-2 rounded hover:bg-emerald-100 font-medium"
          >📅 Đơn Đặt Lịch</a
        >

        <a
          href="/admin/logout"
          class="block px-4 py-2 rounded hover:bg-red-100 text-red-600 font-medium"
          >🚪 Đăng xuất</a
        >
      </nav>
    </aside>

    <!-- Main content -->
    <main class="flex-1 ml-64 p-6 overflow-y-auto">
      <header class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-semibold">
          <%= title || 'Bảng điều khiển' %>
        </h1>
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
        <p class="text-slate-500 text-sm">
          Xin chào, <%= currentUser.name %> 👋
        </p>
        <% } %>
      </header>

      <!-- Nội dung trang -->
      <section><%- body %></section>
    </main>
  </body>
</html>
